@* Renders ChildContent - sub-content is already portaled by Popover. *@
@ChildContent

@code {
    /// <summary>
    /// The content to render (typically DropdownMenuSubContent).
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}

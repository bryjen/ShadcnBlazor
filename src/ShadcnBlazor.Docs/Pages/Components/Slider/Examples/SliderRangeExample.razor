<div class="space-y-2">
    <Slider @bind-Values="_rangeValues" Min="0" Max="100" AriaLabel="Price range" />
    <p class="text-sm text-muted-foreground">Range: @_rangeValues[0] – @_rangeValues[1]</p>
</div>

@code {
    private List<double> _rangeValues = [20, 80];
}
